- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc 

- create:
#    - ~/downloads
#    - ~/.vim/undo-history
    - ~/Projects


# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
