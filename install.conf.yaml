- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc 
    ~/.zshenv: zshenv
    ~/.tmux.conf: 
    ~/.gitconfig:
    ~/.config/bat:
      create: true
      path: config/bat

- create:
#    - ~/downloads
#    - ~/.vim/undo-history
    - ~/Projects


- shell:
#  - [git submodule update --init --recursive, Installing submodules]
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
